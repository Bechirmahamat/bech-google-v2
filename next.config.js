/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "logos-download.com",
            "upload.wikimedia.org",
            "images.unsplash.com",
        ],
    },
};

module.exports = nextConfig;
